---
export interface Props {
	text?: string;
	id?: string;
}

const { text, id } = Astro.props;
---

<h4
	id={id}
	class="flex flex-row items-center gap-2.5 py-4 px-6 text-base font-bold leading-none text-app-text"
>
	{text ? text : <slot />}
</h4>

