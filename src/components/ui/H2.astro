---
export interface Props {
	text?: string;
	id?: string;
}

const { text, id } = Astro.props;
---

<h2
	id={id}
	class="flex flex-row items-center gap-2.5 px-4 md:px-6 py-4 rounded-xl bg-app-bg text-lg font-bold text-app-text"
>
	{text ? text : <slot />}
</h2>

