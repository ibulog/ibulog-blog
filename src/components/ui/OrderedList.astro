---
export interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<ol class={`px-3 text-base font-normal leading-[1.8em] text-app-text list-decimal list-inside space-y-2.5 ${className || ''}`}>
	<slot />
</ol>

