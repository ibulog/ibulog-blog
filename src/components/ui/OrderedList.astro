---
export interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<ol class={`px-4 md:px-6 text-base font-normal text-app-text list-decimal list-inside space-y-2.5 ${className || ''}`}>
	<slot />
</ol>

